lista exacta de features por escena y por usuario

Perfecto. Aquí tienes la **lista exacta y estructurada de features** para que el motor de scoring funcione desde día 1 (sin ML complejo) y además quede preparado para evolucionar a embeddings y modelos predictivos.

La organizo en:

1. 🎭 Features por ESCENA
2. 👤 Features por USUARIO
3. 📊 Features derivadas (interacción usuario–escena)
4. 📈 Señales dinámicas (post-publicación)

---

# 1️⃣ FEATURES POR ESCENA (`SceneFeatures`)

## A. Identidad básica

* `scene_id`
* `work_id`
* `author_id`
* `source_type` → curated | community | unpublished
* `language`
* `original_language`
* `publication_year`
* `copyright_status` → public_domain | licensed | unknown | restricted
* `license_verified` → boolean

---

## B. Clasificación literaria

* `genre_primary` → drama | comedy | romance | thriller | fantasy | sci-fi | historical | literary | etc.
* `genre_secondary` → array
* `tone` → light | dark | ironic | tragic | epic | intimate
* `emotional_intensity` → 1–5
* `emotion_tags` → anger | sadness | joy | nostalgia | tension | fear | hope | etc.
* `theme_tags` → betrayal | love | war | identity | redemption | social conflict | etc.
* `character_archetype` → hero | antihero | villain | narrator | rebel | mentor | etc.

---

## C. Formato interpretativo

* `scene_type` → monologue | dialogue | narration | mixed
* `num_characters_required`
* `dialogue_ratio` (0–1)
* `narration_ratio` (0–1)
* `estimated_duration_seconds`
* `text_length_words`
* `complexity_level` → 1–5 (lenguaje)
* `memorization_difficulty` → 1–5
* `production_complexity` → 1–5 (props, locación, vestuario)

---

## D. Potencial de performance (inicial o histórico)

* `prior_ctr`
* `prior_retention`
* `prior_like_rate`
* `prior_comment_rate`
* `prior_share_rate`
* `completion_rate` (usuarios que la eligieron y publicaron)
* `avg_rating_in_app`
* `impressions_global`
* `unique_creators_count`

---

## E. Discovery / Long Tail

* `author_popularity_index` (normalizado 0–1)
* `work_popularity_index`
* `long_tail_score = 1 / sqrt(1 + impressions_global)`
* `novelty_global_score`
* `is_trending` → boolean
* `trend_velocity` (crecimiento semanal)

---

## F. Seguridad y riesgo

* `sensitive_topics` → violencia | sexual | política | religión | drogas | suicidio | etc.
* `content_risk_score` (0–1)
* `copyright_risk_score`
* `community_spam_risk`
* `moderation_flag_count`

---

## G. Calidad editorial

* `editor_score` (0–1)
* `community_upvotes`
* `community_downvotes`
* `author_verified` (para inéditos)
* `adaptation_quality_score` (evaluación automática)

---

## H. Representación vectorial (opcional pero recomendado)

* `scene_embedding_vector`
* `emotion_embedding_vector`
* `style_embedding_vector`

---

# 2️⃣ FEATURES POR USUARIO (`UserFeatures`)

## A. Perfil creativo

* `user_id`
* `preferred_language`
* `secondary_languages`
* `preferred_genres` (array)
* `preferred_tones`
* `preferred_emotions`
* `preferred_character_archetypes`
* `preferred_scene_type`
* `preferred_duration_range`
* `intensity_preference` (1–5)

---

## B. Restricciones

* `blocked_topics`
* `max_characters_available`
* `max_production_complexity`
* `copyright_sensitivity_level`

---

## C. Capacidad interpretativa

* `experience_level` → beginner | intermediate | advanced
* `memorization_capacity_score`
* `expressiveness_score`
* `voice_variation_score`
* `emotional_range_score`
* `previous_scene_complexity_avg`

---

## D. Performance histórico

* `avg_view_count`
* `avg_retention`
* `avg_like_rate`
* `avg_comment_rate`
* `avg_share_rate`
* `audience_size`
* `growth_velocity`
* `best_performing_genres`
* `best_performing_scene_type`
* `audience_preference_vector` (embedding)

---

## E. Discovery comportamiento

* `novelty_tolerance` (0–1)
* `long_tail_engagement_rate`
* `author_diversity_index`
* `works_seen_history`
* `authors_seen_history`

---

## F. Motivación estratégica

* `goal_mode` → viral | discovery | casting | balanced
* `risk_tolerance`
* `competition_mode` → casual | competitive

---

## G. Reputación interna

* `completion_rate`
* `avg_rating_in_app`
* `community_reputation_score`
* `strike_history`
* `copyright_compliance_score`

---

## H. Representación vectorial

* `user_preference_embedding`
* `user_style_embedding`
* `audience_response_embedding`

---

# 3️⃣ FEATURES DERIVADAS (usuario–escena)

Estas no se almacenan: se calculan al momento del scoring.

* `genre_match_score`
* `tone_match_score`
* `emotion_alignment_score`
* `duration_compatibility_score`
* `complexity_compatibility_score`
* `production_feasibility_score`
* `audience_fit_score`
* `novelty_user_score`
* `long_tail_alignment_score`
* `risk_penalty_score`
* `creator_author_style_alignment` (clave para inéditos)
* `predicted_retention_for_user`
* `predicted_engagement_for_user`

---

# 4️⃣ SEÑALES DINÁMICAS (POST PUBLICACIÓN)

Se actualizan cada vez que alguien publica esa escena.

* `views`
* `retention_curve`
* `like_rate`
* `comment_sentiment_score`
* `share_velocity`
* `subscriber_conversion_rate`
* `watch_time_minutes`
* `audience_overlap_similarity`
* `replay_rate`
* `click_through_from_app`

Estas alimentan:

* `Perf(s)`
* reputación del proponente (community/unpublished)
* ajuste de pesos del algoritmo

---

# 🎯 Resultado

Con estas features puedes:

* Calcular Fit con embeddings + reglas
* Predecir Performance incluso sin data (priors por género/escena)
* Impulsar descubrimiento sin perder calidad
* Filtrar riesgo legal
* Hacer matching autor-creador en obras inéditas
* Crear rankings inteligentes
* Construir modo “casting”

---

Si quieres, el siguiente paso puede ser:

* 🧠 La estructura exacta de tablas en Postgres / DynamoDB
* ⚙️ Cómo normalizar cada feature (escalas 0–1 reales)
* 📊 Cómo inicializar priors sin datos históricos
* 🤖 Cómo migrar esto a un modelo ML cuando tengas volumen

Tú decides hacia dónde lo llevamos.
